#!/bin/bash

# purpose : I create a new repository on github, this is a fast way to initialise my new project
# and sync it with github

git add .
git commit -m 'init' .

# TODO: get current safari url
# TODO: check if it contains github in it
# TODO: if it does, then just use that and write the rest of the instructions
